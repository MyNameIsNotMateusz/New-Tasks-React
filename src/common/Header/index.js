import { HeaderWrapper } from "./styled"

const Header = () => {
    return (
        <HeaderWrapper>
            <h1>To-do list</h1>
            <h4>A simple task list</h4>
        </HeaderWrapper>
    );
};

export default Header;

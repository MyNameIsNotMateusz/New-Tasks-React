import styled from "styled-components";

export const HeaderTaskWrapper = styled.header`
  text-align: center;
  margin: auto;
  margin-top: 200px;
`;

export const DivTaskWrapper = styled.div`
    max-width: 600px;
    border-radius: 10px;
    margin: auto;
    background-color: rgba(0, 255, 255, 0.092);
    padding: 50px;
    font-size: large;
`;